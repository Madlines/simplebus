{
  "name": "madlines-simplebus",
  "version": "2.0.0-0",
  "main": "src/index.js",
  "repository": "github:madlines/simplebus",
  "license": "MIT",
  "description": "It allows you to easily setup Message Buses (like Command Buses and Evens Buses) with handlers and middleware, and run messages through them.",
  "scripts": {
    "lint": "eslint src/**/*.ts ./tests/**/*.ts",
    "test": "mocha --reporter nyan -r ts-node/register \"src/**/*.spec.ts\" \"tests/**/*.spec.ts\"",
    "prepublish": "npm run lint && npm test && npx tsc && npx tsc -d"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  },
  "devDependencies": {
    "@types/chai": "^4.2.3",
    "@types/mocha": "^5.2.7",
    "@types/sinon": "^7.5.0",
    "@typescript-eslint/eslint-plugin": "^2.7.0",
    "@typescript-eslint/parser": "^2.7.0",
    "chai": "^4.2.0",
    "eslint": "^6.6.0",
    "husky": "^3.0.9",
    "mocha": "^6.2.2",
    "sinon": "^7.5.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4"
  },
  "dependencies": {}
}
